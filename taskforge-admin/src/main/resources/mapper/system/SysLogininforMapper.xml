<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhq.taskforge.system.mapper.SysLogininforMapper">

    <resultMap type="com.zhq.taskforge.system.domain.SysLogininfor" id="SysLogininforResult">
        <id property="infoId" column="info_id"/>
        <result property="userName" column="user_name"/>
        <result property="status" column="status"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
        <result property="deptName" column="dept_name"/>
    </resultMap>

    <sql id="selectLogininforVo">
        select info_id, user_name, status, ipaddr, login_location, browser, os, msg, login_time, dept_name
        from sys_logininfor
    </sql>

    <insert id="insertLogininfor" parameterType="com.zhq.taskforge.system.domain.SysLogininfor">
        insert into sys_logininfor(user_name, status, ipaddr, login_location, browser, os, msg, login_time, dept_name)
        values (#{userName}, #{status}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate(), #{deptName})
    </insert>

    <select id="selectLogininforList" parameterType="com.zhq.taskforge.system.domain.SysLogininfor" resultMap="SysLogininforResult">
        <include refid="selectLogininforVo"/>
        <where>
            <if test="ipaddr != null and ipaddr != ''">
                AND ipaddr like concat('%', #{ipaddr}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(login_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(login_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
        order by info_id desc
    </select>

    <delete id="deleteLogininforByIds" parameterType="Long">
        delete from sys_logininfor where info_id in
        <foreach collection="array" item="infoId" open="(" separator="," close=")">
            #{infoId}
        </foreach>
    </delete>

    <select id="selectLogininforById" parameterType="Long" resultMap="SysLogininforResult">
        <include refid="selectLogininforVo"/>
        where info_id = #{infoId}
    </select>

    <update id="cleanLogininfor">
        truncate table sys_logininfor
    </update>

</mapper>
